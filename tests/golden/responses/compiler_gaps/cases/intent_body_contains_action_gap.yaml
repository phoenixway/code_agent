name: intent_body_contains_action_gap
input: |
  <intent mode="activate">{"intent_id":"x","intent_type":"MODIFY","goal":"do <action>bad</action>"}</intent>
legacy_expectation:
  handles: true
  invalid_kind: intent_body_contains_action
compiler_expectation:
  current_status: ready
  expected_shape: INVALID
  expected_error_code: E_PROTOCOL_TAG_IN_JSON_STRING
  notes: The compiler now inspects JSON string values for nested protocol tags.
migration_target:
  desired_compiler_behavior: No change needed.
  priority: high
triage:
  verdict: compiler_false_negative
  reason: Fixed by compiler JSON string protocol-tag validation.
