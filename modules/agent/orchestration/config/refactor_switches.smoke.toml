# Smoke/refactor profile for validating compiler-authority branches.
# Do not use as production default without explicit approval.

[board_checkpoint]
plan_checkpoint_only = "compiler"
plan_checkpoint_with_text = "compiler"
plan_checkpoint_with_action = "compiler"
memory_checkpoint_only = "legacy"
memory_checkpoint_with_text = "legacy"
memory_checkpoint_with_action = "legacy"

[terminal_answer]
plaintext_terminal_answer = "compiler"
checkpoint_only = "legacy"
checkpoint_with_visible_text = "legacy"

[dispatch]
plan_first_single_action = "legacy"

[board_memory]
memory_checkpoint_only = "compiler"
memory_checkpoint_with_text = "compiler"

[recovery]
compiler_invalid_kind_mapping = "compiler"
prevalidation_reject_invalid_output = "compiler"
leaked_system_result = "compiler"
invalid_truncated_terminal_text = "legacy"
